from setuptools import setup, find_packages

setup(
    name="dimensionality_reduction",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # зависимости
    ],
    author="Author",
    description="Dimensionality Reduction Algorithms",
)